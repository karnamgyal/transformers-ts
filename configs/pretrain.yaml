seed: 41

device: cuda  

data:
  n_samples: 2000
  seq_len: 512
  n_channels: 3
  batch_size: 8
  num_workers: 0

model:
  patch_len: 16
  stride: 16
  d_model: 128
  n_heads: 4
  n_layers: 2

train:
  lr: 0.0003
  epochs: 10
  log_interval: 10

ssl:
  mask_ratio: 0.75
